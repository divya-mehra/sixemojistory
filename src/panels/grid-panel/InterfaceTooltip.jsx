import { Button, Tooltip } from "@mui/material";

const InterfaceTooltip = () => {
  return (
    <Tooltip title="This is a tooltip">
      <Button>Hover me</Button>
    </Tooltip>
  );
};

export default InterfaceTooltip;
