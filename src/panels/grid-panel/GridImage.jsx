const GridImage = () => {
  return (
    <img
      style={{

        // opacity: 1,
        // zIndex: 1000,
      }}
      src={`/images/pocketbell.png`}
      alt=" Image"
    ></img>
  );
};

export default GridImage;
